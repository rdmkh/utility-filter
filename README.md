# :wrench: Утилита фильтрации данных из текстовых файлов

Консольное приложение для чтения текстовых файлов, 
автоматического определения типа данных каждой строки 
(целое число, вещественное число или строка), 
разделения их по соответствующим выходным файлам и вывода статистики обработки.

## :clipboard: Содержимое
- [Требования к окружению](#-требования-к-окружению)
- [Опции командной строки](#-требования-к-окружению)
- [Примеры запуска приложения](#-примеры-запуска-приложения)
- [Выходные файлы](#-выходные-файлы)
- [Сторонние библиотеки](#-сторонние-библиотеки)

---

## ✅ Требования к окружению

- Java 21+
- Maven 3.9+

---

## ⚙️ Опции командной строки


- ```-a``` - режим добавления в существующие файлы (по умолчанию – перезапись.
- ```-p <префикс>``` - префикс для имён результирующих файлов (по умолчанию пустая строка).
- ```-o <путь>``` - папка для сохранения результатов (по умолчанию текущая директория).
- ```-s``` - краткая статистика (только количество элементов).
- ```-f``` - полная статистика (min, max, сумма, среднее, длины строк).

**Важно:** опции -s и -f взаимоисключающие. При одновременном указании программа выдаст ошибку.

---

## 🚀 Примеры запуска приложения
```dtd
> java -jar utility-filter.jar data1.txt data2.txt
> java -jar utility-filter.jar -s -p log- data1.txt data2.txt
> java -jar utility-filter.jar -f -a data1.txt data2.txt
> java -jar utility-filter.jar -o ../results -p sample_ data1.txt
```

---

## 📂 Выходные файлы

По умолчанию файлы с результатами располагаются в текущей папке с именами integers.txt, floats.txt, strings.txt.

- Префикс (```-p```) добавляется перед именем файла.
- Директория (```-o```) задаёт папку для сохранения результатов.
- Режим добавления (```-a```) дописывает строки в конец существующего файла, иначе файл перезаписывается.

---

## 📦 Сторонние библиотеки

- JCommander 3.0

```xml 
<dependency>
    <groupId>org.jcommander</groupId>
    <artifactId>jcommander</artifactId>
    <version>3.0</version>
</dependency>
```

---